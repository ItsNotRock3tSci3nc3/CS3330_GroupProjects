package edu.mu.adopt_me.model;

/**
 * A cat that can be adopted.
 */
public class Cat extends Pet {
	public Cat(String id, String species, String name, int age) {
		super(id, "Cat", species, name, age);
	}
}
